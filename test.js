// Fonction pour récupérer la valeur saisie dans la modal et l'afficher
function getValue() {
    // Récupérer la valeur saisie dans le champ input de la modal
    var value = document.getElementById("exampleFormControlInput1").value;
    // Afficher la valeur saisie dans la console
    console.log("La valeur saisie est :", value);
    // Vous pouvez également faire autre chose avec cette valeur, par exemple l'afficher dans une autre partie de votre page
}
